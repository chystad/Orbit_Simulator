# These configuration options are currently in configs/default.yaml
BASILISK_SIMULATION:
  useSphericalHarmonics: true
  orbitCase: GEO # GEO, GTO, LEO (default)
  show_plots: true
  show_progress_bar: true
  deltaT: 10 # Solver time step [seconds]
  override_skf_initial_state: false # might rename to something with 'override' 